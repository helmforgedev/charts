# SPDX-License-Identifier: Apache-2.0
externalSecrets:
  enabled: true
  items:
    - spec:
        dataFrom:
          - extract:
              key: netbird
            sourceRef:
              storeRef:
                name: fake
          - extract:
              key: netbird-extra
