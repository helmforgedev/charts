# SPDX-License-Identifier: Apache-2.0
postgresql:
  enabled: false
database:
  mode: external
  external:
    engine: mysql
    host: mysql.example.com
    port: 3306
    name: netbird
    username: netbird
    password: external-password
