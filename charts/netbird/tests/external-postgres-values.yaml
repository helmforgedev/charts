# SPDX-License-Identifier: Apache-2.0
postgresql:
  enabled: false
database:
  mode: external
  external:
    engine: postgres
    host: postgres.example.com
    port: 5432
    name: netbird
    username: netbird
    password: external-password
    sslMode: require
