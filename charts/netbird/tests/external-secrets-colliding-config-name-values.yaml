# SPDX-License-Identifier: Apache-2.0
externalSecrets:
  enabled: true
  items:
    - spec:
        secretStoreRef:
          name: fake
        data:
          - secretKey: netbird_SMTP_PASSWORD
            remoteRef:
              key: netbird/smtp
    - name: secret
      spec:
        secretStoreRef:
          name: fake
        data:
          - secretKey: netbird_SMTP_PASSWORD
            remoteRef:
              key: netbird/named-secret
