# SPDX-License-Identifier: Apache-2.0
replicaCount: 1
ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: apache.example.local
      paths:
        - path: /
          pathType: Prefix
