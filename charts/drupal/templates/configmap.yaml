{{- if .Values.php.ini }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "drupal.configMapName" . }}
  labels:
    {{- include "drupal.labels" . | nindent 4 }}
data:
  custom.ini: |
{{ .Values.php.ini | indent 4 }}
{{- end }}
