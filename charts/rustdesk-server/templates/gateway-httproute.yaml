{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{- if .Values.gatewayAPI.enabled }}
{{- $names := dict }}
{{- range .Values.gatewayAPI.httpRoutes | default list }}
{{- $name := include "rustdesk-server.httpRouteName" (dict "root" $ "route" .) }}
{{- if hasKey $names $name }}{{- fail (printf "gatewayAPI.httpRoutes renders duplicate resource name %s; use distinct names after truncation" $name) }}{{- end }}
{{- $_ := set $names $name true }}
{{- $port := add (int $.Values.server.rendezvousPort) 2 }}
{{- if eq (.component | default "hbbs") "hbbr" }}{{- $port = add (int $.Values.server.relayPort) 2 }}{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $name }}
  labels:
    {{- include "rustdesk-server.labels" $ | nindent 4 }}
    {{- with .labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .parentRefs }}
  parentRefs:
    {{- toYaml .parentRefs | nindent 4 }}
  {{- end }}
  {{- with .hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
  {{- if .rules }}
    {{- range .rules }}
    - {{- with .matches }}
      matches:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .filters }}
      filters:
        {{- toYaml .filters | nindent 8 }}
      {{- end }}
      {{- if .backendRefs }}
      backendRefs:
        {{- toYaml .backendRefs | nindent 8 }}
      {{- else if not .omitDefaultBackend }}
      backendRefs:
        - name: {{ include "rustdesk-server.fullname" $ }}
          port: {{ $port }}
      {{- end }}
    {{- end }}
  {{- else }}
    - backendRefs:
        - name: {{ include "rustdesk-server.fullname" $ }}
          port: {{ $port }}
  {{- end }}
{{- end }}
{{- end }}
