# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API (GR-060)
master:
  persistence:
    enabled: false

data:
  persistence:
    enabled: false

gateway:
  enabled: true
  hostnames:
    - elasticsearch.local
