# CI: external PostgreSQL and Redis
postgresql:
  enabled: false

redis:
  enabled: false

database:
  external:
    host: db.example.com
    port: "5432"
    name: automatisch
    username: automatisch_user
    password: "ci-password"

redis_config:
  external:
    host: redis.example.com
    port: "6379"
