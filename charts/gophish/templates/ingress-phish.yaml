{{- include "gophish.validateAll" . }}
{{- if .Values.phishIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "gophish.fullname" . }}-phish
  labels:
    {{- include "gophish.labels" . | nindent 4 }}
    app.kubernetes.io/component: phish
  {{- with .Values.phishIngress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.phishIngress.ingressClassName }}
  ingressClassName: {{ .Values.phishIngress.ingressClassName }}
  {{- end }}
  {{- if .Values.phishIngress.tls }}
  tls:
    {{- range .Values.phishIngress.tls }}
    - secretName: {{ .secretName }}
      hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.phishIngress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range (.paths | default (list (dict "path" "/" "pathType" "Prefix"))) }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "gophish.fullname" $ }}-phish
                port:
                  name: phish
          {{- end }}
    {{- end }}
{{- end }}
