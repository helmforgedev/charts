# Gophish with external MySQL.
# Use case: keep database lifecycle outside the application release.

database:
  mode: external
  external:
    host: mysql.database.svc.cluster.local
    port: 3306
    name: gophish
    username: gophish
    existingSecret: gophish-db
    existingSecretDsnKey: dsn
