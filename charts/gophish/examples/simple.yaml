# Gophish simple local-style deployment.
# Use case: single-replica SQLite deployment with admin access through port-forward.
# Admin access:
# kubectl port-forward svc/<release>-gophish-admin 3333:3333

replicaCount: 1

persistence:
  enabled: true
  size: 5Gi
