# Backup scenario placeholder. Backup resources are implemented in a later phase.
backup:
  enabled: true
  schedule: "*/15 * * * *"
  s3:
    endpoint: http://minio.minio.svc.cluster.local:9000
    bucket: gophish-backups
    prefix: gophish
    existingSecret: gophish-backup
