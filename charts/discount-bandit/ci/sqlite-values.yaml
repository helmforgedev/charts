# SPDX-License-Identifier: Apache-2.0
# CI: explicit SQLite development mode
database:
  mode: sqlite
  sqlite:
    enabled: true
mysql:
  enabled: false
persistence:
  database:
    enabled: true
    size: 1Gi
