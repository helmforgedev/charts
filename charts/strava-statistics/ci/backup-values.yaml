backup:
  enabled: true
  suspend: true
  schedule: "0 3 * * *"
  archivePrefix: strava-statistics
  s3:
    endpoint: http://minio.minio.svc:9000
    bucket: strava-backups
    prefix: daily
    accessKey: minioadmin
    secretKey: minioadmin

strava:
  clientId: "12345"
  clientSecret: "test-secret"
  refreshToken: "test-token"
