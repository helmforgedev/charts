# SPDX-License-Identifier: Apache-2.0
persistence:
  userdata:
    existingClaim: openhab-shared-data
    subPath: userdata
  conf:
    existingClaim: openhab-shared-data
    subPath: conf
  addons:
    existingClaim: openhab-shared-data
    subPath: addons
