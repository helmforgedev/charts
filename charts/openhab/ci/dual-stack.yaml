# SPDX-License-Identifier: Apache-2.0
# CI scenario: k3d-safe IP family policy for HTTP service (GR-058)
# Unit tests keep explicit IPv4/IPv6 dual-stack render coverage.

service:
  ipFamilyPolicy: SingleStack
  ipFamilies:
    - IPv4
