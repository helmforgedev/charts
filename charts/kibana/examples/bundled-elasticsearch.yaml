# SPDX-License-Identifier: Apache-2.0
bundledElasticsearch:
  enabled: true

bundled-elasticsearch:
  clusterProfile: dev
  image:
    tag: "9.5.1"
  kibana:
    enabled: false
  master:
    persistence:
      enabled: false
  sysctlInit:
    enabled: false
