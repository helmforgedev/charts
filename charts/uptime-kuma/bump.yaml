chart: uptime-kuma

upstream:
  registry: docker.io
  repository: louislam/uptime-kuma
  tagFilter: "^\d+\.\d+\.\d+$"
  excludeFilter: "-rc|-beta|-alpha|-pr"

values:
  imageKey: image.tag

helm:
  testArgs: >-
    --set replicaCount=1
    --set persistence.enabled=false
