# SPDX-License-Identifier: Apache-2.0
auth:
  dummyPassword: local-password

gateway:
  enabled: true
  parentRefs:
    - name: public
      namespace: gateway-system
  hostnames:
    - hub.example.com
