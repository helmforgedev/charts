# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute (GR-060)
gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - metabase.example.com
  path: /
  pathType: PathPrefix
