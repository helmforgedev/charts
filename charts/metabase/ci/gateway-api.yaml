# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute (GR-060)
postgresql:
  enabled: false

database:
  external:
    host: "postgres.example.com"
    name: metabase
    username: metabase
    password: "testpassword"

gateway:
  enabled: true
  gatewayName: envoy-gateway
  gatewayNamespace: envoy-gateway
  hostnames:
    - metabase.example.com
  path: /
  pathType: PathPrefix
