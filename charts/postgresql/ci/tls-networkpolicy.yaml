# SPDX-License-Identifier: Apache-2.0
architecture: replication

tls:
  enabled: true
  existingSecret: postgresql-tls

networkPolicy:
  enabled: true

replication:
  primary:
    persistence:
      enabled: false
  readReplicas:
    replicaCount: 2
    persistence:
      enabled: false
