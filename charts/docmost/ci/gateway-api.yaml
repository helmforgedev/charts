# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API (GR-060)
postgresql:
  standalone:
    persistence:
      enabled: false

redis:
  standalone:
    persistence:
      enabled: false

gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - docmost.local
