database:
  mode: external
  external:
    host: pg.example.internal
    name: docmost
    username: docmost
    password: change-me

postgresql:
  enabled: false

redis:
  enabled: false
  external:
    host: redis.example.internal
    password: change-me

storage:
  mode: s3
  s3:
    bucket: docmost
    endpoint: https://minio.example.internal
    accessKey: minio
    secretKey: secret123
