visibility:
  mode: allowlist
  enableTags:
    - public
    - approved
  disableTags:
    - destructive

sources:
  inline:
    tools:
      visible.py: |
        __tags__ = {"public"}

        def visible() -> str:
            return "visible"
