# SPDX-License-Identifier: Apache-2.0
persistence:
  enabled: false
gateway:
  enabled: true
  parentRefs:
    - name: public-gateway
      namespace: gateway-system
  hostnames:
    - jenkins.example.local
