# SPDX-License-Identifier: Apache-2.0
persistence:
  enabled: false
rbac:
  create: false
  readSecrets: false
networkPolicy:
  enabled: true
  egress:
    enabled: true
    allowCluster: true
    allowDns: true
    allowInternet: false
pdb:
  enabled: true
plugins:
  install:
    enabled: true
jcasC:
  enabled: true
  configScripts:
    welcome: |
      jenkins:
        systemMessage: "Managed by HelmForge"
