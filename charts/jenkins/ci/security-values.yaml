# SPDX-License-Identifier: Apache-2.0
persistence:
  enabled: false
rbac:
  create: true
  readSecrets: true
pdb:
  enabled: true
plugins:
  install:
    enabled: true
jcasC:
  enabled: true
  configScripts:
    welcome: |
      jenkins:
        systemMessage: "Managed by HelmForge"
