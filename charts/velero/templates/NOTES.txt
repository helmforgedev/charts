Velero has been installed.

Useful checks:

  kubectl get deployment {{ include "velero.fullname" . }} -n {{ .Release.Namespace }}
  kubectl get backupstoragelocations.velero.io -n {{ .Release.Namespace }}
  kubectl get schedules.velero.io -n {{ .Release.Namespace }}

If filesystem backup is enabled:

  kubectl get daemonset {{ include "velero.fullname" . }}-node-agent -n {{ .Release.Namespace }}

Important:

  - validate object storage connectivity before relying on this release for backups
  - if you use S3-compatible storage such as MinIO, review the s3Url and s3ForcePathStyle settings
  - if you define schedules, confirm they target the intended namespaces and resource sets
