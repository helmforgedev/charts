# SPDX-License-Identifier: Apache-2.0
# CI: Gateway API HTTPRoute rendering
gateway:
  enabled: true
  parentRefs:
    - name: shared-gateway
      namespace: ingress
  hostnames:
    - cd.example.com
