# SPDX-License-Identifier: Apache-2.0
secrets:
  adminDefaultPassword: CiIngressAdmin2026!
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: medikeep.example.test
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: medikeep-tls
      hosts:
        - medikeep.example.test
