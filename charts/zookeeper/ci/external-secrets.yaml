# SPDX-License-Identifier: Apache-2.0
persistence:
  enabled: false
auth:
  client:
    enabled: true
externalSecrets:
  enabled: true
  secretStoreRef:
    name: platform-secrets
  data:
    - secretKey: jaas.conf
      remoteRef:
        key: zookeeper/jaas
