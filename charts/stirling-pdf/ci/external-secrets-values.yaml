# SPDX-License-Identifier: Apache-2.0
fullnameOverride: stirling-pdf
auth:
  existingSecret: stirling-pdf-eso-auth
externalSecrets:
  enabled: true
  items:
    - fullnameOverride: stirling-pdf-eso-auth
      spec:
        secretStoreRef:
          name: helmforge-fake-store
          kind: ClusterSecretStore
        data:
          - secretKey: password
            remoteRef:
              key: test/password
