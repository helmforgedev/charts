# SPDX-License-Identifier: Apache-2.0
phpmyadmin:
  host: mysql.default.svc.cluster.local

gatewayAPI:
  enabled: true
  parentRefs:
    - name: public
      namespace: gateway-system
  hostnames:
    - pma.example.local
