# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute (GR-060)
persistence:
  enabled: false

gateway:
  enabled: true
  parentRefs:
    - name: envoy
      namespace: envoy-gateway
  hostnames:
    - ntfy.example.com
  path: /
  pathType: PathPrefix
