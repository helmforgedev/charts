# SPDX-License-Identifier: Apache-2.0
# Values used to render CRDs for server-side apply before upgrading the release.
crds:
  gatewayAPI:
    enabled: true
  envoyGateway:
    enabled: true
safeUpgradePolicy:
  management: disabled
