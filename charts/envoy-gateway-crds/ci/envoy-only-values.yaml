# SPDX-License-Identifier: Apache-2.0
# Provider-managed Gateway API with only Envoy Gateway extension CRDs installed.
crds:
  gatewayAPI:
    enabled: false
  envoyGateway:
    enabled: true
safeUpgradePolicy:
  management: external
