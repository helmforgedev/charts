# SPDX-License-Identifier: Apache-2.0
# Bootstrap or migration render with both CRD bundles and no policy resources.
crds:
  gatewayAPI:
    enabled: true
  envoyGateway:
    enabled: true
safeUpgradePolicy:
  management: disabled
