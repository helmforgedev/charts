# SPDX-License-Identifier: Apache-2.0
# Full CRD bundle while a platform release owns the safe-upgrade policy.
crds:
  gatewayAPI:
    enabled: true
  envoyGateway:
    enabled: true
safeUpgradePolicy:
  management: external
