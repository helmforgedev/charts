# SPDX-License-Identifier: Apache-2.0
imaginary:
  enabled: true
notifyPush:
  enabled: true
postgresql:
  auth:
    existingSecret: nextcloud-integrations-database
redis:
  auth:
    existingSecret: nextcloud-integrations-redis
networkPolicy:
  enabled: true
  # Official signed Client Push release download for the installation test.
  extraEgress:
    - ports: [{port: 443, protocol: TCP}]
