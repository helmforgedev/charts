# SPDX-License-Identifier: Apache-2.0
nextcloud:
  trustedDomains: [cloud.example.com]
  overwriteCliUrl: https://cloud.example.com
  overwriteProtocol: https
imaginary:
  enabled: true
notifyPush:
  enabled: true
# Install/enable the Client Push app and run occ notify_push:setup after deployment.
# See docs/integrations.md for routing, network policy and app lifecycle details.
