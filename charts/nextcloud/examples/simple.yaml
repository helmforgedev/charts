# SPDX-License-Identifier: Apache-2.0
# Local access with kubectl port-forward on port 8080.
nextcloud:
  trustedDomains: [localhost]
  overwriteCliUrl: http://localhost:8080
persistence:
  size: 10Gi
