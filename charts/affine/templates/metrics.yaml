{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "affine.fullname" . | trunc 55 | trimSuffix "-" }}-metrics
  labels: {{ include "affine.labels" . | nindent 4 }}
spec:
  {{- if .Values.service.ipFamilyPolicy }}
  ipFamilyPolicy: {{ .Values.service.ipFamilyPolicy }}
  {{- end }}
  {{- with .Values.service.ipFamilies }}
  ipFamilies: {{ toYaml . | nindent 4 }}
  {{- end }}
  ports:
    - {name: metrics, port: {{ .Values.metrics.port }}, targetPort: metrics}
  selector: {{ include "affine.selectorLabels" . | nindent 4 }}
{{- if .Values.metrics.serviceMonitor.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "affine.fullname" . }}
  labels:
    {{- include "affine.labels" . | nindent 4 }}
    {{- with .Values.metrics.serviceMonitor.labels }}{{ toYaml . | nindent 4 }}{{- end }}
spec:
  selector:
    matchLabels: {{ include "affine.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.metrics.serviceMonitor.interval }}
      scrapeTimeout: {{ .Values.metrics.serviceMonitor.scrapeTimeout }}
{{- end }}
{{- if .Values.metrics.prometheusRule.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: {{ include "affine.fullname" . }}
  labels:
    {{- include "affine.labels" . | nindent 4 }}
    {{- with .Values.metrics.prometheusRule.labels }}{{ toYaml . | nindent 4 }}{{- end }}
spec:
  groups:
    - name: {{ include "affine.fullname" . }}.native
      rules:
        - alert: AFFiNEMetricsUnavailable
          expr: up{namespace={{ .Release.Namespace | quote }},service={{ printf "%s-metrics" (include "affine.fullname" . | trunc 55 | trimSuffix "-") | quote }}} == 0
          for: 5m
          labels: {severity: warning}
          annotations: {summary: "AFFiNE native metrics target is unavailable"}
        {{- with .Values.metrics.prometheusRule.additionalRules }}{{ toYaml . | nindent 8 }}{{- end }}
{{- end }}
{{- end }}
