# CI: external PostgreSQL database
postgresql:
  enabled: false

database:
  mode: external
  external:
    host: postgres.example.com
    port: 5432
    name: listmonk
    username: listmonk
    password: changeme
    sslMode: require
