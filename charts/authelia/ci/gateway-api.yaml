# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute (GR-060)
persistence:
  enabled: false

gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - auth.example.com
  path: /
  pathType: PathPrefix
