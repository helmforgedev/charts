# CI: external PostgreSQL database
postgresql:
  enabled: false

database:
  external:
    host: db.example.com
    port: "5432"
    name: chiefonboarding
    username: onboard_user
    password: "ci-password"
