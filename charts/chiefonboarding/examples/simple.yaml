# SPDX-License-Identifier: Apache-2.0
# Basic ChiefOnboarding deployment with bundled PostgreSQL.

chiefonboarding:
  baseUrl: http://localhost:8000
  secretKey: "replace-with-a-stable-django-secret-key"

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
