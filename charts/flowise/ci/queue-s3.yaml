architecture:
  mode: queue

flowise:
  replicaCount: 2

persistence:
  enabled: false

postgresql:
  enabled: true

redis:
  enabled: true

storage:
  type: s3
  s3:
    bucketName: flowise
    accessKeyId: minio
    secretAccessKey: secret123
    region: us-east-1
    endpointUrl: http://minio.minio.svc.cluster.local:9000
    forcePathStyle: true
