# SPDX-License-Identifier: Apache-2.0
database:
  mode: postgresql

postgresql:
  enabled: true
  standalone:
    persistence:
      enabled: false

storage:
  type: local

persistence:
  enabled: true
