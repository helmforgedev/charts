database:
  mode: external
  external:
    vendor: postgres
    host: postgres.example.internal
    name: flowise
    username: flowise
    password: change-me

storage:
  type: local

persistence:
  enabled: true
