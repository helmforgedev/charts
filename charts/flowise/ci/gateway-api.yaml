# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API (GR-060)
persistence:
  enabled: false
postgresql:
  standalone:
    persistence:
      enabled: false

gateway:
  enabled: true
  hostnames:
    - flowise.local
  parentRefs:
    - name: my-gateway
      namespace: default
