{{/* SPDX-License-Identifier: Apache-2.0 */}}
Flowise has been deployed.

Service:
  HTTP: {{ include "flowise.fullname" . }}:{{ .Values.service.port }}
{{- if include "flowise.appUrl" . }}
Configured application URL:
  {{ include "flowise.appUrl" . }}
{{- else }}
Set flowise.appUrl or enable ingress for a stable external URL.
{{- end }}
