{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Flowise Helm Chart Values",
  "description": "Configuration values for the Flowise Helm chart with standalone SQLite mode or scalable queue mode using Redis and PostgreSQL.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "nameOverride": { "type": "string", "description": "Override the chart name used in resource names" },
    "fullnameOverride": { "type": "string", "description": "Override the full release name used in resource names" },
    "commonLabels": { "type": "object", "description": "Labels added to every resource created by this chart" },
    "image": {
      "type": "object",
      "description": "Flowise image settings",
      "properties": {
        "repository": { "type": "string", "description": "Flowise container image repository" },
        "tag": { "type": "string", "description": "Flowise image tag. Defaults to appVersion validated on GitHub Releases and Docker Hub." },
        "pullPolicy": { "type": "string", "description": "Image pull policy", "enum": ["Always", "IfNotPresent", "Never"] }
      }
    },
    "imagePullSecrets": { "type": "array", "description": "Image pull secrets for private registries", "items": { "type": "object" } },
    "architecture": {
      "type": "object",
      "description": "Flowise architecture settings",
      "properties": {
        "mode": { "type": "string", "description": "Flowise architecture mode: standalone | queue", "enum": ["standalone", "queue"] }
      }
    },
    "flowise": {
      "type": "object",
      "description": "Main Flowise application settings",
      "properties": {
        "replicaCount": { "type": "integer", "description": "Number of main Flowise server replicas" },
        "appUrl": { "type": "string", "description": "Public application URL. Auto-detected from the first ingress host when empty." },
        "logLevel": { "type": "string", "description": "Log level: error | warn | info | verbose | debug" },
        "numberOfProxies": { "type": "integer", "description": "Rate-limit proxy count when running behind ingress or load balancers" },
        "corsOrigins": { "type": "string", "description": "Allowed CORS origins" },
        "iframeOrigins": { "type": "string", "description": "Allowed iframe origins" },
        "fileSizeLimit": { "type": "string", "description": "Maximum upload size accepted by Flowise" },
        "showCommunityNodes": { "type": "boolean", "description": "Show community nodes in the UI" },
        "disableTelemetry": { "type": "boolean", "description": "Disable telemetry reporting" },
        "disabledNodes": { "type": "string", "description": "Disable selected node names, comma-separated" },
        "extraEnv": { "type": "array", "description": "Extra environment variables for the main Flowise container", "items": { "type": "object" } }
      }
    },
    "auth": { "type": "object", "description": "Authentication and encryption secret settings" },
    "database": {
      "type": "object",
      "description": "Database selection and external database settings",
      "properties": {
        "mode": { "type": "string", "description": "Database mode: auto | sqlite | external | postgresql", "enum": ["auto", "sqlite", "external", "postgresql"] },
        "sqlite": { "type": "object", "description": "SQLite settings" },
        "external": { "type": "object", "description": "External database settings" }
      }
    },
    "postgresql": { "type": "object", "description": "PostgreSQL subchart configuration" },
    "queue": { "type": "object", "description": "Queue mode settings including worker replicas and concurrency" },
    "redis": { "type": "object", "description": "Redis subchart or external Redis configuration" },
    "storage": {
      "type": "object",
      "description": "Blob storage settings",
      "properties": {
        "type": { "type": "string", "description": "Storage type: local | s3", "enum": ["local", "s3"] },
        "local": { "type": "object", "description": "Local blob storage settings" },
        "s3": { "type": "object", "description": "S3-compatible blob storage settings" }
      }
    },
    "persistence": { "type": "object", "description": "Persistent storage settings for local Flowise data" },
    "service": { "type": "object", "description": "Service configuration for the Flowise web application" },
    "ingress": { "type": "object", "description": "Ingress configuration for Flowise" },
    "startupProbe": { "type": "object", "description": "Startup probe for the main Flowise server" },
    "livenessProbe": { "type": "object", "description": "Liveness probe for the main Flowise server" },
    "readinessProbe": { "type": "object", "description": "Readiness probe for the main Flowise server" },
    "resources": { "type": "object", "description": "CPU and memory requests/limits for the main Flowise container" },
    "podSecurityContext": { "type": "object", "description": "Pod-level security context" },
    "securityContext": { "type": "object", "description": "Container-level security context" },
    "serviceAccount": { "type": "object", "description": "ServiceAccount configuration" },
    "priorityClassName": { "type": "string", "description": "Priority class name" },
    "podLabels": { "type": "object", "description": "Labels added to the main Flowise pod" },
    "podAnnotations": { "type": "object", "description": "Annotations added to the main Flowise pod" },
    "nodeSelector": { "type": "object", "description": "Node selector for pod scheduling" },
    "tolerations": { "type": "array", "description": "Tolerations for pod scheduling", "items": { "type": "object" } },
    "affinity": { "type": "object", "description": "Affinity rules for pod scheduling" },
    "extraVolumeMounts": { "type": "array", "description": "Additional volume mounts for the main Flowise container", "items": { "type": "object" } },
    "extraVolumes": { "type": "array", "description": "Additional volumes for the Flowise pods", "items": { "type": "object" } },
    "extraManifests": { "type": "array", "description": "Extra Kubernetes manifests to deploy alongside the chart", "items": { "type": "object" } },
    "backup": {
      "type": "object",
      "description": "Scheduled PostgreSQL backup to S3-compatible storage",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Enable scheduled PostgreSQL backups to S3-compatible storage"
        },
        "schedule": {
          "type": "string",
          "description": "Cron schedule for backups (default: daily at 03:00 UTC)"
        },
        "suspend": {
          "type": "boolean",
          "description": "Suspend the CronJob without deleting it"
        },
        "concurrencyPolicy": {
          "type": "string",
          "description": "Concurrency policy: Forbid, Replace, Allow",
          "enum": ["Forbid", "Replace", "Allow"]
        },
        "successfulJobsHistoryLimit": {
          "type": "integer",
          "description": "Number of successful job records to keep"
        },
        "failedJobsHistoryLimit": {
          "type": "integer",
          "description": "Number of failed job records to keep"
        },
        "backoffLimit": {
          "type": "integer",
          "description": "Maximum retries per backup job"
        },
        "archivePrefix": {
          "type": "string",
          "description": "Prefix for backup archive filenames"
        },
        "images": {
          "type": "object",
          "description": "Container images used by backup jobs",
          "properties": {
            "postgresql": { "type": "string", "description": "Image used for PostgreSQL backup (must have pg_dump)" },
            "uploader": { "type": "string", "description": "Image used for S3 upload (MinIO client)" }
          }
        },
        "resources": {
          "type": "object",
          "description": "Resources for backup containers"
        },
        "database": {
          "type": "object",
          "description": "Override pg_dump arguments for backup",
          "properties": {
            "pgDumpArgs": { "type": "string", "description": "Extra arguments passed to pg_dump" }
          }
        },
        "s3": {
          "type": "object",
          "description": "S3-compatible storage configuration",
          "properties": {
            "endpoint": { "type": "string", "description": "S3-compatible endpoint URL" },
            "bucket": { "type": "string", "description": "Target bucket name" },
            "prefix": { "type": "string", "description": "Key prefix within the bucket" },
            "createBucketIfNotExists": { "type": "boolean", "description": "Auto-create the bucket if it does not exist" },
            "existingSecret": { "type": "string", "description": "Use an existing secret for S3 credentials" },
            "existingSecretAccessKeyKey": { "type": "string", "description": "Key in the existing secret for the access key" },
            "existingSecretSecretKeyKey": { "type": "string", "description": "Key in the existing secret for the secret key" },
            "accessKey": { "type": "string", "description": "Inline S3 access key (ignored when existingSecret is set)" },
            "secretKey": { "type": "string", "description": "Inline S3 secret key (ignored when existingSecret is set)" }
          }
        }
      }
    },
    "gateway": {
      "type": "object",
      "description": "Gateway API HTTPRoute configuration.",
      "properties": {
        "enabled": { "type": "boolean", "default": false },
        "annotations": { "type": "object" },
        "parentRefs": { "type": "array", "items": { "type": "object" } },
        "hostnames": { "type": "array", "items": { "type": "string" } },
        "path": { "type": "string", "default": "/" },
        "pathType": { "type": "string", "enum": ["PathPrefix", "Exact", "RegularExpression"], "default": "PathPrefix" }
      }
    },
    "externalSecrets": {
      "type": "object",
      "description": "External Secrets Operator configuration.",
      "properties": {
        "enabled": { "type": "boolean", "default": false },
        "apiVersion": { "type": "string", "default": "external-secrets.io/v1" },
        "refreshInterval": { "type": "string", "default": "1h" },
        "secretStoreRef": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "kind": { "type": "string" }
          }
        },
        "target": {
          "type": "object",
          "properties": {
            "creationPolicy": { "type": "string" }
          }
        },
        "data": { "type": "array", "items": { "type": "object" } }
      }
    }
  }
}
