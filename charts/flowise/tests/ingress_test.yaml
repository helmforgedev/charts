suite: Ingress
templates:
  - templates/ingress.yaml
tests:
  - it: renders ingress when enabled
    values:
      - ingress-values.yaml
    asserts:
      - equal:
          path: spec.ingressClassName
          value: traefik
      - equal:
          path: spec.rules[0].host
          value: flowise.example.com
