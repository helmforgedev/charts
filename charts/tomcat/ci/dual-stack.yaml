# SPDX-License-Identifier: Apache-2.0
# CI scenario: k3d-safe IP family policy for the single-stack validation lab.
# Unit tests keep explicit IPv4/IPv6 dual-stack render coverage.
service:
  ipFamilyPolicy: SingleStack
  ipFamilies:
    - IPv4
