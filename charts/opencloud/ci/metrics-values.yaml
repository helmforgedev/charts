# SPDX-License-Identifier: Apache-2.0
fullnameOverride: opencloud-metrics
metrics:
  enabled: true
  ingressFrom:
    - podSelector:
        matchLabels: {app: opencloud-monitor}
  serviceMonitor:
    enabled: true
    interval: 5s
    scrapeTimeout: 3s
  prometheusRule:
    enabled: true
persistence:
  size: 1Gi
