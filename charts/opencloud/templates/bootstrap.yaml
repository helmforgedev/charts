{{/* SPDX-License-Identifier: Apache-2.0 */}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "opencloud.fullname" . | trunc 53 | trimSuffix "-" }}-bootstrap
  labels: {{ include "opencloud.labels" . | nindent 4 }}
data:
  bootstrap.sh: |
{{ .Files.Get "files/bootstrap.sh" | indent 4 }}
