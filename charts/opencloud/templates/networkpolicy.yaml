{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "opencloud.fullname" . }}
  labels: {{ include "opencloud.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels: {{ include "opencloud.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    {{- if .Values.networkPolicy.egressIsolation }}
    - Egress
    {{- end }}
  ingress:
    - from:
        {{- if .Values.networkPolicy.ingressFrom }}
        {{- toYaml .Values.networkPolicy.ingressFrom | nindent 8 }}
        {{- else }}
        - podSelector: {}
        {{- end }}
      ports:
        - protocol: TCP
          port: http
    {{- if .Values.metrics.enabled }}
    - from:
        {{- if .Values.metrics.ingressFrom }}
        {{- toYaml .Values.metrics.ingressFrom | nindent 8 }}
        {{- else }}
        - podSelector: {}
        {{- end }}
      ports: [{protocol: TCP, port: metrics}]
    {{- end }}
  {{- if .Values.networkPolicy.egressIsolation }}
  egress:
    - to:
        - podSelector:
            matchLabels: {{ include "opencloud.selectorLabels" . | nindent 14 }}
      ports:
        - {protocol: TCP, port: {{ .Values.server.port }}}
    {{- if .Values.networkPolicy.dnsEgress }}
    - to: {{ toYaml .Values.networkPolicy.dnsEgress | nindent 8 }}
      ports:
        - {protocol: UDP, port: 53}
        - {protocol: TCP, port: 53}
    {{- end }}
    {{- if .Values.networkPolicy.webEgress }}
    - to: {{ toYaml .Values.networkPolicy.webEgress | nindent 8 }}
      ports:
        {{- range .Values.networkPolicy.webPorts }}
        - {protocol: TCP, port: {{ . }}}
        {{- end }}
    {{- end }}
    {{- with .Values.networkPolicy.extraEgress }}{{ toYaml . | nindent 4 }}{{- end }}
  {{- end }}
{{- end }}
