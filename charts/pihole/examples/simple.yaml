# Simple Pi-hole deployment with default upstream DNS (Google)
# Suitable for quick setup on a home network

serviceDns:
  type: LoadBalancer
  loadBalancerIP: "192.168.1.53"

pihole:
  timezone: UTC

admin:
  password: "change-me"
