# CI: Staging profile with cert-manager
# Tests staging profile with self-signed certificates
profile: staging

certificates:
  certManager:
    enabled: true
    issuer: selfsigned
    issuerKind: ClusterIssuer

gatewayAPI:
  examples:
    enabled: true
