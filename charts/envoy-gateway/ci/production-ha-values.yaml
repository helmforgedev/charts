# CI: Production HA profile
# Tests production-ha profile with DaemonSet and HA features
profile: production-ha

gatewayAPI:
  examples:
    enabled: false

highAvailability:
  enabled: true
  podDisruptionBudget:
    minAvailable: 1
