# Simple Envoy Gateway deployment with dev profile
# Use case: Local development and testing on k3d, kind, or minikube

profile: dev

gatewayAPI:
  examples:
    enabled: true

proxy:
  service:
    type: LoadBalancer
