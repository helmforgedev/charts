# SPDX-License-Identifier: Apache-2.0
fullnameOverride: memos-mysql
database:
  driver: mysql
mysql:
  enabled: true
  auth:
    password: "fixture-only:@%/?#&=+ 2026"
  standalone:
    persistence:
      size: 1Gi
    resources:
      requests: {cpu: 100m, memory: 256Mi}
      limits: {cpu: "1", memory: 1Gi}
persistence:
  size: 1Gi
