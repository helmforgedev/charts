# SPDX-License-Identifier: Apache-2.0
fullnameOverride: memos-postgresql
database:
  driver: postgres
postgresql:
  enabled: true
  auth:
    password: "fixture-only:@%/?#&=+ 2026"
  standalone:
    persistence:
      size: 1Gi
    resources:
      requests: {cpu: 100m, memory: 128Mi}
      limits: {cpu: "1", memory: 512Mi}
persistence:
  size: 1Gi
