# SPDX-License-Identifier: Apache-2.0
mode: production

hostname:
  hostname: https://sso.example.com

database:
  tls:
    enabled: true
    sslMode: disable

postgresql:
  enabled: true
  auth:
    database: keycloak
    username: keycloak
    password: testpassword
