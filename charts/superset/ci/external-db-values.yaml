# SPDX-License-Identifier: Apache-2.0
# CI scenario - PostgreSQL and Redis integration
postgresql:
  enabled: true
  auth:
    database: superset
    username: superset

redis:
  enabled: true

database:
  mode: subchart

redisConfig:
  mode: subchart
