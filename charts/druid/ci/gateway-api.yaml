# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute for the Druid router (GR-060)
gatewayAPI:
  enabled: true
  hostnames:
    - druid.example.com
  paths:
    - type: PathPrefix
      value: /

