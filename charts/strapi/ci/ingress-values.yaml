# SPDX-License-Identifier: Apache-2.0
# CI: Ingress enabled
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: cms.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: strapi-tls
      hosts:
        - cms.example.com
