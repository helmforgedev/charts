# SPDX-License-Identifier: Apache-2.0
gatewayApi:
  enabled: true
  httpRoutes:
    - name: web
      parentRefs:
        - name: my-gateway
          namespace: default
      hostnames:
        - strapi.example.com
      rules:
        - matches:
            - path:
                type: PathPrefix
                value: /
