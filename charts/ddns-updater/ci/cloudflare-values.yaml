# CI: Cloudflare provider configuration
config:
  settings:
    - provider: cloudflare
      zone_identifier: "fake-zone-id"
      domain: "example.com"
      host: "@"
      ttl: 300
      token: "fake-api-token"
      proxied: false
      ip_version: "ipv4"
