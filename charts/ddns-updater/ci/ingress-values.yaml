# SPDX-License-Identifier: Apache-2.0
# CI: ingress enabled
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: ddns.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - ddns.example.com
      secretName: ddns-tls
