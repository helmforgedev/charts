# CI: ingress enabled
ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - host: appwrite.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: appwrite-tls
      hosts:
        - appwrite.example.com
