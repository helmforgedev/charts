# Standalone Appwrite with default MariaDB and Redis subcharts
ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: appwrite.example.com
      paths:
        - path: /
          pathType: Prefix
  # tls:
  #   - secretName: appwrite-tls
  #     hosts:
  #       - appwrite.example.com
