apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "appwrite.fullname" . }}-api
  labels:
    {{- include "appwrite.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.api.replicaCount }}
  selector:
    matchLabels:
      {{- include "appwrite.componentLabels" (dict "root" . "component" "api") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "appwrite.componentLabels" (dict "root" . "component" "api") | nindent 8 }}
      {{- with .Values.api.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "appwrite.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      containers:
        - name: api
          image: {{ include "appwrite.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["php", "-e", "app/http.php", "-dopcache.preload=opcache.preload"]
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            {{- include "appwrite.commonEnv" . | nindent 12 }}
          volumeMounts:
            {{- include "appwrite.sharedVolumeMounts" . | nindent 12 }}
          {{- include "appwrite.httpLivenessProbe" . | nindent 10 }}
          {{- include "appwrite.httpReadinessProbe" . | nindent 10 }}
          {{- include "appwrite.httpStartupProbe" . | nindent 10 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          {{- with .Values.api.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        {{- include "appwrite.sharedVolumes" . | nindent 8 }}
      {{- with .Values.api.nodeSelector | default .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.tolerations | default .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.api.affinity | default .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
