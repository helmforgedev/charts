externalSecrets:
  enabled: true
  items:
    - name: test-secret
      spec:
        refreshInterval: 1h
        target:
          name: test-secret
        storeRef:
          name: test-store
          kind: ClusterSecretStore
        data:
          - secretKey: key
            remoteRef:
              key: remote-key
