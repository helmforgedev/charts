# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API HTTPRoute (GR-060)
persistence:
  conf:
    enabled: false
  work:
    enabled: false

gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - adguard.example.com
  path: /
  pathType: PathPrefix
