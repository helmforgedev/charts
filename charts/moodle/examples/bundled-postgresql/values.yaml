# SPDX-License-Identifier: Apache-2.0
# Merge with your production routing, administrator Secret and storage values.
database:
  type: postgresql
postgresql:
  enabled: true
mysql:
  enabled: false
mariadb:
  enabled: false
