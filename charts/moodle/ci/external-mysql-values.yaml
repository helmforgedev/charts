# SPDX-License-Identifier: Apache-2.0
postgresql:
  enabled: false
database:
  type: mysql
  host: moodle-external-mysql
  port: 3307
  existingSecret: moodle-external-mysql
  existingSecretPasswordKey: application-password
  mysqlSslMode: verify-full
  tlsSecret: moodle-external-mysql
  connectTimeout: 600
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    interval: 15s
    scrapeTimeout: 10s
  prometheusRule:
    enabled: true
networkPolicy:
  enabled: true
  extraEgress:
    - to:
        - podSelector:
            matchLabels:
              app: moodle-external-mysql
      ports:
        - {port: 3307, protocol: TCP}
    - to:
        - podSelector:
            matchLabels:
              app: moodle-monitor
      ports:
        - {port: 9090, protocol: TCP}
