backup:
  enabled: true
  suspend: true
  schedule: "0 3 * * *"
  archivePrefix: wallabag
  s3:
    endpoint: http://minio.minio.svc:9000
    bucket: wallabag-backups
    prefix: daily
    accessKey: minioadmin
    secretKey: minioadmin
