chart: vaultwarden

upstream:
  registry: docker.io
  repository: vaultwarden/server
  tagFilter: "^\d+\.\d+\.\d+$"
  excludeFilter: "-rc|-beta|-alpha|-testing"

values:
  imageKey: image.tag

helm:
  testArgs: >-
    --set replicaCount=1
    --set persistence.enabled=false
