# SPDX-License-Identifier: Apache-2.0
gateway:
  enabled: true
  parentRefs:
    - name: shared-gateway
      namespace: gateway-system
  hostnames:
    - dash.example.local
  path: /
  pathType: PathPrefix
