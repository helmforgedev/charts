# SPDX-License-Identifier: Apache-2.0
externalSecrets:
  enabled: true
  items:
    - spec:
        secretStoreRef:
          name: fake
        data:
          - secretKey: CERTIMATE_SMTP_PASSWORD
            remoteRef:
              key: certimate/smtp
    - name: secret
      spec:
        secretStoreRef:
          name: fake
        data:
          - secretKey: CERTIMATE_SMTP_PASSWORD
            remoteRef:
              key: certimate/named-secret
