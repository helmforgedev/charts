# SPDX-License-Identifier: Apache-2.0
fullnameOverride: redis-tls
architecture: standalone
tls:
  enabled: true
  existingSecret: fixture-redis-tls
service:
  ports:
    redis: 6380
standalone:
  persistence:
    enabled: false
