# SPDX-License-Identifier: Apache-2.0
fullnameOverride: reactive-resume-production
server:
  publicUrl: https://resume.example.test
serviceAccount:
  name: reactive-resume-production
postgresql:
  serviceAccount:
    create: false
    name: reactive-resume-production
networkPolicy:
  ingressFrom:
    - podSelector: {}
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-system
