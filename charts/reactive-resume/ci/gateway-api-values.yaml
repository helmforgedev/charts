# SPDX-License-Identifier: Apache-2.0
fullnameOverride: reactive-resume-gateway
server:
  publicUrl: https://resume.example.test
gatewayAPI:
  enabled: true
  httpRoutes:
    - parentRefs:
        - name: shared
          namespace: gateway-system
      hostnames:
        - resume.example.test
