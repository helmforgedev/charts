{{/* SPDX-License-Identifier: Apache-2.0 */}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pocket-id.fullname" . | trunc 53 | trimSuffix "-" }}-bootstrap
  labels: {{ include "pocket-id.labels" . | nindent 4 }}
data:
  bootstrap.mjs: |
{{ .Files.Get "files/bootstrap.mjs" | indent 4 }}

  database.mjs: |
{{ .Files.Get "files/database.mjs" | indent 4 }}
  database-admission.sh: |
{{ .Files.Get "files/database-admission.sh" | indent 4 }}
