# SPDX-License-Identifier: Apache-2.0
# Immich exposed with Gateway API

gateway:
  enabled: true
  parentRefs:
    - name: public
      namespace: gateway-system
  hostnames:
    - immich.example.com
  path: /
  pathType: PathPrefix

networkPolicy:
  enabled: true
