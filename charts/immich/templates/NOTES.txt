Immich has been deployed.

Service:
  {{ include "immich.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Health:
  {{ .Values.probes.path }}

Image:
  {{ .Values.image.repository }}:{{ .Values.image.tag }}
