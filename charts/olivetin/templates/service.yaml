apiVersion: v1
kind: Service
metadata:
  name: {{ include "olivetin.fullname" . }}
  labels:
    {{- include "olivetin.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "olivetin.selectorLabels" . | nindent 4 }}
