{{/* SPDX-License-Identifier: Apache-2.0 */}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "olivetin.fullname" . }}-config
  labels:
    {{- include "olivetin.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- if .Values.configTpl.enabled }}
    {{- tpl .Values.config . | nindent 4 }}
    {{- else }}
    {{- .Values.config | nindent 4 }}
    {{- end }}
