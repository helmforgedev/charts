backup:
  enabled: true
  suspend: true
  schedule: "0 3 * * *"
  archivePrefix: umami
  s3:
    endpoint: http://minio.minio.svc:9000
    bucket: umami-backups
    prefix: daily
    accessKey: minioadmin
    secretKey: minioadmin
