backup:
  enabled: true
  suspend: true
  schedule: "0 3 * * *"
  archivePrefix: ckan
  s3:
    endpoint: http://minio.minio.svc:9000
    bucket: ckan-backups
    prefix: daily
    accessKey: minioadmin
    secretKey: minioadmin
