# SPDX-License-Identifier: Apache-2.0
# CI scenario - PostgreSQL, Redis, and Solr integration
postgresql:
  enabled: true
  auth:
    database: ckan
    username: ckan

redis:
  enabled: true

solr:
  enabled: true

database:
  mode: subchart

redisConfig:
  mode: subchart

datapusher:
  enabled: false
