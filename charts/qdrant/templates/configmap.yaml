{{/* SPDX-License-Identifier: Apache-2.0 */}}
{{- if .Values.config.localYaml }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qdrant.fullname" . }}
  labels:
    {{- include "qdrant.labels" . | nindent 4 }}
data:
  local.yaml: |
{{ .Values.config.localYaml | nindent 4 }}
{{- end }}
