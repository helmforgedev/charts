backup:
  enabled: true
  suspend: true
  schedule: "0 3 * * *"
  archivePrefix: middleware
  s3:
    endpoint: http://minio.minio.svc:9000
    bucket: middleware-backups
    prefix: daily
    accessKey: minioadmin
    secretKey: minioadmin
