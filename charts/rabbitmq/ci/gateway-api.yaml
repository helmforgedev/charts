# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API (GR-060)

management:
  enabled: true

gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - rabbitmq.local
