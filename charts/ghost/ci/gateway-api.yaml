# SPDX-License-Identifier: Apache-2.0
# CI scenario: Gateway API (GR-060)
persistence:
  enabled: false
mysql:
  enabled: false

gateway:
  enabled: true
  parentRefs:
    - name: my-gateway
      namespace: default
  hostnames:
    - ghost.local
